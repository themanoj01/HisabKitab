@page "/transaction"
@using HisabKitab.Model
@using Microsoft.AspNetCore.Components.Forms
@using System.ComponentModel.DataAnnotations

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transactions</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
        }

        .form-control, .form-select {
            border-radius: 8px;
        }

        .btn-primary {
            border-radius: 8px;
        }

        footer {
            text-align: center;
            margin-top: 20px;
            font-size: 14px;
            color: #888;
        }

        .form-floating > label {
            padding: 0.5rem 0.75rem;
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <h3 class="mb-3">Add Transaction</h3>
        <p class="text-muted">Fill out the details below to add a transaction.</p>

        <EditForm Model="@transaction" OnValidSubmit="HandleTransactionSubmit" class="row g-3">
            <DataAnnotationsValidator />
            <ValidationSummary />

            <!-- Amount -->
            <div class="col-md-6 form-floating">
                <InputNumber class="form-control" id="amount" @bind-Value="transaction.Amount" required />
                <label for="amount">Amount</label>
            </div>

            <!-- Transaction Type -->
            <div class="col-md-6 form-floating">
                <InputSelect class="form-select" id="type" @bind-Value="transaction.Type" required>
                    @foreach (var type in Enum.GetValues<TransactionType>())
                    {
                        <option value="@type">@type</option>
                    }
                </InputSelect>
                <label for="type">Transaction Type</label>
            </div>

            <!-- Default Tags -->
            <div class="col-md-6 form-floating">
                <InputSelect class="form-select" id="tag" @bind-Value="transaction.DefaultTags" required>
                    <option value="">Select Tag</option>
                    @foreach (var tag in Enum.GetValues<DefaultTags>())
                    {
                        <option value="@tag">@tag</option>
                    }
                </InputSelect>
                <label for="tag">Default Tag</label>
            </div>

            <!-- Notes -->
            <div class="col-12 form-floating">
                <textarea class="form-control" id="notes" @bind="transaction.Notes" rows="3" placeholder="Add any relevant details"></textarea>
                <label for="notes">Notes</label>
            </div>

            <!-- Submit Button -->
            <div class="col-12 d-flex justify-content-center mb-4">
                <button type="submit" class="btn btn-primary w-50">+ Add Transaction</button>
            </div>
        </EditForm>
        <h3> Recent Transactions </h3>
        <div class="filter-section">
            <div class="d-flex justify-content-between mb-3">
                <button class="btn btn-filter">Filter by Type</button>
                <button class="btn btn-filter">Filter by Tags</button>
                <button class="btn btn-filter">Filter by Date</button>
                <button class="btn btn-primary">Search by Title</button>
                <button class="btn btn-sort">Sort by Date</button>
            </div>
        </div>
            <table class="table table-hover table-bordered text-center align-middle mt-4">
                <thead class="table-dark">
                    <tr>
                        <th>S.N.</th>
                        <th>Amount</th>
                        <th>Type</th>
                        <th>Date</th>
                        <th>Notes</th>
                    </tr>
                </thead>
                <tbody>
                    @if (transactions != null && transactions.Any())
                    {
                        int serial = 1;
                        foreach (var txn in transactions)
                        {
                            <tr>
                                <td>@serial</td>
                                <td>@txn.Amount</td>
                                <td>@txn.Type</td>
                                <td>@txn.Date.ToString("yyyy-MM-dd")</td>
                                <td>@txn.Notes</td>
                            </tr>
                            serial++;
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5">No transactions found.</td>
                        </tr>
                    }
                </tbody>
            </table>
        <!-- Footer -->
        <footer>
            &copy; 2025 HisabKitab. All Rights Reserved.
        </footer>
    </div>
</body>
</html>


